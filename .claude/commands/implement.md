---
description: Implement the current in-progress feature
---

Implement the feature that is currently in-progress.

## Instructions

1. Check `features/in-progress/` for the current feature
2. If no feature is in-progress, list backlog and ask which to start
3. Read the feature file thoroughly
4. Follow the implementation steps in the feature file
5. Check off each step as you complete it `[x]`
6. Run any tests specified in the Testing section
7. When done, ask if the user wants to mark it complete with `/feature-done`

## Rules

- Follow the feature file exactly - don't add unrequested changes
- Update the feature file checkboxes as you progress
- If you encounter blockers, document them in the feature file
